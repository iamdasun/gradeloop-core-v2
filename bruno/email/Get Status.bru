meta {
  name: Get Email Status
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/emails/status/:id
  body: none
  auth: none
}

params:path {
  id: {{email_id}}
}

vars:pre-request {
  base_url: http://localhost:8082
}
