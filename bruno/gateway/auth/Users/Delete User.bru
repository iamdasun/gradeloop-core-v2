meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{gateway_url}}/api/v1/users/:id
  body: none
  auth: bearer
}

params:path {
  id: USER_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}
