meta {
  name: Get User
  type: http
  seq: 3
}

get {
  url: {{gateway_url}}/api/v1/users/:id
  body: none
  auth: bearer
}

params:path {
  id: USER_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}
