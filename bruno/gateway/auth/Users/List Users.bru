meta {
  name: List Users
  type: http
  seq: 2
}

get {
  url: {{gateway_url}}/api/v1/users?page=1&limit=10
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 10
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  gateway_url: http://localhost:8080
}
