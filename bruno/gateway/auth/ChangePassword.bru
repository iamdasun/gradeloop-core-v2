meta {
  name: Change Password
  type: http
  seq: 7
}

post {
  url: {{gateway_url}}/api/v1/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "old_password": "OldPassword123!",
    "new_password": "NewPassword123!"
  }
}
