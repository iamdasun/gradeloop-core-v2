meta {
  name: Reset Password
  type: http
  seq: 5
}

post {
  url: {{gateway_url}}/api/v1/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "RESET_TOKEN_HERE",
    "new_password": "newpassword123"
  }
}
