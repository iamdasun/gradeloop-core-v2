meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{gateway_url}}/api/v1/users/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "designation": "Senior Developer"
  }
}
