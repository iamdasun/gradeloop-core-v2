meta {
  name: Compare Two Codes
  type: http
  seq: 2
}

post {
  url: {{CIPAS_SYNTACTICS_URL_V1}}/compare
  body: json
  auth: inherit
}

body:json {
  {
    "code1": "public int foo(int x) { return x + 1; }",
    "code2": "public int bar(int y) { return y + 1; }",
    "language": "java"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
