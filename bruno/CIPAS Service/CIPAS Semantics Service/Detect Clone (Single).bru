meta {
  name: Detect Clone (Single)
  type: http
  seq: 4
}

post {
  url: {{CIPAS_SEMANTICS_URL_V1}}/detect
  body: json
  auth: inherit
}

body:json {
  {
    "code1": "def add(a, b): return a + b",
    "code2": "def sum_two(x, y):\n    result = x + y\n    return result"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
