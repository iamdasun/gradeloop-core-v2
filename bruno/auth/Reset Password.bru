meta {
  name: Reset Password
  type: http
  seq: 11
}

post {
  url: {{auth_url}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "REPLACE_WITH_TOKEN_FROM_EMAIL_OR_DB",
    "new_password": "newpassword123"
  }
}
