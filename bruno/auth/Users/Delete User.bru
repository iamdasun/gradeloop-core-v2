meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{auth_url}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: USER_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}
