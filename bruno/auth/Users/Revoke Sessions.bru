meta {
  name: Revoke Sessions
  type: http
  seq: 2
}

post {
  url: {{auth_url}}/admin/users/:id/revoke-sessions
  body: none
  auth: bearer
}

params:path {
  id: USER_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}
