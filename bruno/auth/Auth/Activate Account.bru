meta {
  name: Activate Account
  type: http
  seq: 6
}

post {
  url: {{auth_url}}/auth/activate
  body: json
  auth: none
}

body:json {
  {
    "token": "ACTIVATION_TOKEN",
    "password": "NewPassword123!"
  }
}
