meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{auth_url}}/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
