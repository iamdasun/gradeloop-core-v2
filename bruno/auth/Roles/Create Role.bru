meta {
  name: Create Role
  type: http
  seq: 1
}

post {
  url: {{auth_url}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "NEW_ROLE",
    "description": "Description of the new role"
  }
}
