meta {
  name: Delete Role
  type: http
  seq: 5
}

delete {
  url: {{auth_url}}/roles/:id
  body: none
  auth: bearer
}

params:path {
  id: ROLE_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}
