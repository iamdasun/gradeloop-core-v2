meta {
  name: Update Role
  type: http
  seq: 4
}

put {
  url: {{auth_url}}/roles/:id
  body: json
  auth: bearer
}

params:path {
  id: ROLE_ID_HERE
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "UPDATED_ROLE_NAME",
    "permission_ids": []
  }
}
