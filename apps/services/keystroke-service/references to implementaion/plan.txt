Change the plan if needed, ask me if possible.


🏗️ Phase 1: Architectural Foundation (Storage & Messaging)The current system relies on in-memory dictionaries and unencrypted pickle files, which are not scalable or resilient.1. Database Implementation (Dockerized)Add these services to your docker-compose.yml:Redis: For high-speed, transient session storage (keystroke buffering).PostgreSQL: For persistent user templates, enrollment data, and final session archives.RabbitMQ: (Already in stack) For streaming authentication and anomaly events.2. Scalable Storage LogicSession Buffering: Instead of active_sessions = {}, use Redis with a TTL (Time-To-Live). This allows multiple service instances to access the same session data.Template Persistence: Migrate user_templates.pkl to a PostgreSQL table (user_biometrics). Store the 128-dimensional embeddings as a vector (using pgvector) or a binary blob to ensure concurrent-safe access.🛠️ Phase 2: Claude Code Instructions (The "Robust Enrollment" Logic)You must instruct Claude Code to implement a Multi-Condition Enrollment Workflow. This captures the student's typing signature under different mental states to ensure the model generalizes across the emotional spectrum.Prompt for Claude Code:Task: Implement a robust enrollment controller in main.py and typenet_inference.py based on the provided research.Requirements:Multi-Condition Data Collection: Enrollment must not be a single session. Implement a 4-phase sequence:
* Baseline: 15 repetitions of username/password.
* Transcription: Student types a provided 10-line code snippet to capture "pure" motor rhythm.
* Stress Induction: A 2-minute timed "FizzBuzz" or math-coding task to label "stress" markers.
* Cognitive Load: A complex logic function to capture high mental effort patterns.
2.  Behavioral Envelope Modeling: Modify enroll_user to compute a "behavioral envelope" (the range of rhythms) rather than a single mean template.
3.  Data Threshold: Ensure at least 8 distinct sessions are collected over the initial period to account for temporal and psychological variations.
4.  Feature Prioritization: Weight Dwell Time ($HT$) more heavily during verification, as it is the most stress-invariant feature.📊 Phase 3: Monitoring & Instructor TimelineTo enable instructors to view potential impersonation in a timeline, you need a "Risk Event Stream."1. The Message Streamer (RabbitMQ)Event Generation: Every 70-100 keystrokes, the service must perform a background check.Event Payload:JSON{
  "type": "AUTH_CHECK",
  "studentId": "id",
  "sessionId": "id",
  "similarity": 0.82,
  "isImpersonated": false,
  "offsetSeconds": 145, // Time into the session
  "anomalyType": "velocity_fluctuation" // [cite: 20]
}
2. Frontend Implementation (React/Next.js)Real-time Timeline Component: Create a component that consumes these events via WebSocket.Instructor View:Display a linear timeline of the coding session.Highlight "Friction Points" (struggling with logic) in Yellow.Highlight "Authentication Failures" (potential impersonation) in Red.Note: Use the trace logs to distinguish between a student struggling with material (logical patterns) and a hijacked session (rhythm patterns).💾 Phase 4: Post-Submission Data ArchivingAfter the assignment is submitted, all raw keystroke events must be moved from transient Redis storage to permanent PostgreSQL storage for forensic review.Instructions for Claude Code:Task: Create a FinalizeSession endpoint.Bulk Move: Fetch all buffered events for the sessionId from Redis.Relational Storage: Save to a keystroke_archives table. Store as a compressed JSONB blob or a structured time-series to optimize for 50+ students.Metadata: Link the archive to the assignmentId and studentId for historical analysis.🚀 Summary Checklist for ImplementationComponentTechnologyActionSession CacheRedisReplace active_sessions dict; set 2-hour TTL.User DBPostgreSQLStore biometric templates and session archives.Inference FixPythonImplement the missing continuous_authentication() method.EnrollmentPython/APIImplement the 4-phase "Stress/Cognitive/Baseline" workflow.Instructor UIReactBuild a timeline using the offsetSeconds from auth events.